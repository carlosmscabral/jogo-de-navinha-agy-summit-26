# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
/tmp/booth_session

# Environment variables & local databases
.env
.env.local
.env.*.local
*.sqlite
*.sqlite-journal

# IDE & OS
.DS_Store
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Isolated workspaces for subagent-driven-development
.worktrees/

# Diagnostic output from `npm run sim:balance` (Task B7), regenerated on every run
sim-results.json

# Cópia local de @jogo/shared para o contexto de build do Docker (Tarefa C3) —
# gerada por `npm run vendor:shared --workspace=packages/cloud-api`, nunca commitada.
packages/cloud-api/vendor/

# Firebase — .firebaserc names a real project (vibe-cabral); .firebaserc.example is the template
.firebaserc
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/
